#最先加载的配置文件，被加载后参数不会被覆盖
#spring:
#  cloud:
#    consul:
#      config:
#        enabled: true    # 启用配置中心
#        format: yaml     # 指定配置格式为 yaml
#        data-key: mysql # 也就是 consul 中 key/value 中的 key
#        prefix: config         # 可以理解为配置文件所在的最外层目录
#        defaultContext: demo  # 可以理解为 mysql_config 的上级目录
